# Guard mode - Multi-modal security monitoring via Ollama VLM
-r base.txt

# HTTP client for Ollama API (included in base.txt via requests)
# aiohttp>=3.9.0  # (uncomment if switching to async in the future)
